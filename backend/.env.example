<<<<<<< HEAD
# Environment
ENVIRONMENT=development

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/bookyard

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-key-change-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# CORS - No quotes, just comma-separated
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Logging
LOG_LEVEL=INFO
=======
# ==========================================
# Backend Configuration Example
# Copy this file to .env and fill in the values
# ==========================================

# --- API Configuration ---
# API_HOST: The interface to bind to (0.0.0.0 for Docker/EC2, 127.0.0.1 for local)
API_HOST=0.0.0.0
API_PORT=8000
# API_RELOAD: Set to true for development (hot-reload), false for production
API_RELOAD=true

# --- Database Configuration (Supabase or PostgreSQL) ---
# MANUAL SETUP REQUIRED:
# 1. Login to your Supabase Dashboard: https://supabase.com/dashboard/
# 2. Go to Project Settings -> Database -> Connection String (URI)
# 3. Copy the URI and replace [PASSWORD] with your actual database password.
# Format: postgresql://[USER]:[PASSWORD]@[HOST]:[PORT]/[DATABASE]
DATABASE_URL=postgresql://postgres:INSERT_YOUR_PASSWORD_HERE@db.project_ref.supabase.co:5432/postgres

# --- Security ---
# ADMIN_SECRET: A manual secret key you define to protect bulk data loading.
# You must provide this key in the 'x-admin-secret' header when making requests.
# Generate a strong random string (e.g., `openssl rand -hex 32` or just a long password).
ADMIN_SECRET=PLEASE_CHANGE_ME_TO_A_SECURE_RANDOM_STRING

# --- Logging ---
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
>>>>>>> b005a34 (feat: Set up SQLModel ORM with Book, User, and UserRating models, implement bulk data loading, and add Docker Compose configuration.)
